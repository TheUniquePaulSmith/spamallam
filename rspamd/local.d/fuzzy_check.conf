# Query the public rspamd.com fuzzy storage (free service).
rule "rspamd.com" {
  servers = "round-robin:fuzzy.rspamd.com:11445";
  encryption_key = "icy63itbhhni8bq15ntp5n5symuixf73s1kpjh6skaq4e7nx5fiy";
  symbol = "FUZZY_UNKNOWN";
  mime_types = ["*"];
  max_score = 20.0;
  read_only = true;       # public storage is read-only for us
  skip_unknown = true;
  fuzzy_map = {
    FUZZY_DENIED { max_score = 20.0; flag = 1; }
    FUZZY_PROB   { max_score = 10.0; flag = 2; }
    FUZZY_WHITE  { max_score =  2.0; flag = 3; }
  }
}
