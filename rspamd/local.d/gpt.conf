# The rspamd GPT plugin is deliberately DISABLED — AI analysis is performed by
# the spamallam service instead, by default before rspamd sees the message
# (admin-configurable on the AI settings page; see rspamd.local.lua for how
# rspamd folds the AI verdict into its own score when AI ran first).
enabled = false;
